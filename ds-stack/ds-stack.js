//first in last out
var Stack = function() {
	obj={};
	obj.push
	obj.pop
	obj.array=[]
	obj.msize=0;

	return obj
};
	obj.push=function(value){
		this.msize++
		this.array.push(value)
	}
	obj.pop=function(){
		this.msize--
		if (array) {
			this.array.slice(0,this.value)
		}
		return 'The stack is empty'
	}